import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MODUL09_32688',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
